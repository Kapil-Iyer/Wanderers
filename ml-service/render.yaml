# Render: set Root Directory to parent (Fahh) so model/ is available
# Build: cd ml-service && pip install -r requirements.txt
# Start: cd ml-service && PYTHONPATH=.. uvicorn main:app --host 0.0.0.0 --port $PORT
